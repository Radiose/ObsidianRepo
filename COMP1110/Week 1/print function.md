---
{}
---
print function
Java 25 uses`IO.println` as its main print statement. All lines in a function need a semicolon to finish it. `IO.print` will not add an new line after the print statement.   \n will create a new line in the middle of a string. so `"Hello \n Hello"` will output Hello
											  Hello
\\n will actually add \n into the code 