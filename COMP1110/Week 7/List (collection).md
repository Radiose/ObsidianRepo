List (collection)
### Definition
This is an extension (`extends`) of  [[Collection (interface)]].
It functions similar to a list in other programming languages. It must have its type [[Class]] specified through `<>`.

### Some methods 
`list.add(); ` - adds an item to the end of the list