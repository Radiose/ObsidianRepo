Stack 
A stack is defined as a *last in first out* [[Queue]]. It has the same methods as a [[Queue]]

It has its own unique [[Method]] names, being *push* and *pop*

A [[Linked list]] is a good example of a stack. 

Whenever you have anything to do with [[Recursion]], it will implement some form of stack 