---
aliases:
  - correlation
  - coefficient of correlation
---
the covariance between $X$ and $Y$, denoted $cov(X,Y)=E(XY)-E(X)E(Y)$

Note we are using joint [[random variable|expected value]]s.

note that by definition, $cov(X,X)=E(XX)-E(X)E(X)=var(X)$


Additionally, the coefficient of correlation between $X$ and $Y$ is defined as 
$\rho(X,Y)=\frac{cov(X,Y)}{\sqrt{ Var(X)Var(Y) }}$, which is always in $[-1,1]$. 

A -1 means there is a strong negative correlation and +1 means that there is a strong positive correlation. **A 0 means that there is no correlation** (this is also called zero correlation).


![[variance]]