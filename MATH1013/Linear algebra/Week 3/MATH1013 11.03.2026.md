---
{}
---
review

```handdrawn-ink
{
	"versionAtEmbed": "0.3.4",
	"filepath": "attachments/Ink/Drawing/2026.3.11 - 9.08am.drawing",
	"width": 1156.0025634765625,
	"aspectRatio": 0.9120804216443272
}
```




Therefore, to understand if a set of [[vector]] [[span]] $\mathbb{R}^n$, you can simply check if there are pivots in each row of the [[Matrix]]. As seen in the example above, if h = 2, then there is a CONSTRAINT on the [[span]]. A constraint on a span means that it cannot cover the entire plane, so therefore, the vectors will span r^3 if and only if h/=3.


Another method you can use to tell if vectors span the entire dimensions, is if v1+v2 = v3 or some other combination. This shows that v3 lies on the same plane as v1 and v2, and that therefore, it doesnt span 3 dimensions. 



![[inner products]]

![[linearity]]

![[parametric vector form]]






Equivalent questions
Given a [[vector]] $\vec{b}$, is it a [[Linear combination of vectors]] ($\vec{a_{1}},\vec{a_{2}}\dots \vec{a_{m}}$)
Given a [[vector]] $\vec{b}$, is it in span {$\vec{a_{1}},\vec{a_{2}}\dots \vec{a_{m}}$}
Does the vector equation $x_{1}a_{1},x_{2}a_{2}\dots x_{m}a_{m}$=$\vec{b}$ have a solution 
does the matrix equation A = $[\begin{matrix}\vec{a}_{1},\vec{a}_{2},\dots\vec{m}\end{matrix}]$ have a solution
does the matrix equation $A \vec{x}=\vec{b}$ have a solution where A = $\begin{matrix}[a_{1},a_{2},a_{m}]\end{matrix}$ and $\vec{x}=\begin{pmatrix}x_{1}  \\ x_{2}  \\ x_{3}\end{pmatrix}$?
all these questions are asking about the existence of a solution for a system




If i know that [[vector]] $\vec{b}$ is a [[Linear combination of vectors]], can $\vec{b}$ be **uniquely** written as a linear combination?

Suppose existence is known, what about uniqueness

![[General solution to a nonhomogenous system]]


Uniqueness 
A system of linear equations has infinitely many solutions if there is a free variable present in the REF 

Take the question 
Does $A \vec{x} = \vec{0}$ have 1 solution or infinitely many
Ans: There is only 1 solution if and only if in the REF there are columns without pivots. 
![[linear relation]]

