integrable 
A [[Function]] is integrable if  the [[limits|limit]] definition of the [[Definite integral]] applies over the entire [[Function]].

It is easy to prove a function is not integrable if we assume the function is integrable, and utilise a [[proof by contradiction]].

If f is a [[continuous function]] on \[a, b], or if f has only finitely many jump
discontinuities, then f is integrable on \[a, b]



[[theorem]]
if f is [[integrable]] on \[a,b], and 

P1,P2,P3... is any [[Sequence]] of [[partition]]s of [a,b] such that $\lim_{ n \to \infty } ||P_{n}||=0$  and 
$S_{1},S_{2},S_{3}\dots$ is any sequence such that for each i, $S_{i}$ is a [[Set]] of sample points associated to P

then $\int _a^b f(x)dx = \lim_{ n \to \infty }R(f,P_{n},S_{n})$

A [[partition]] is regular if all of its subgroups are of equal size 

If a function is [[integrable]] on \[a,b], then $\int _a^b f(x)dx$ = -$\int _b^a f(x)dx$

