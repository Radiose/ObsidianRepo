---
aliases:
  - call stack
  - memory heap
---
The *call stack* is the location where java frames are stored. A frame is the data associated with a single call of a function, which java will add onto the call stack. 

The heap is where variables that are called with `new` are placed.