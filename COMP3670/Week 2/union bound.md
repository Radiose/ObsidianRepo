The union bound is a generalisation we can make from the [[set counting|inclusion-exclusion principle]] and the sum rule.

$\mathbb{P}(U_{i=1}^M a_{i})\leq \sum_{i=1}^m\mathbb{P}(A_{i})$
In other words, if none of $m$ things is likely, then it is not likely any will happen.