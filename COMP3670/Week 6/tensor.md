Image datapoints for computer vision tasks for example, are better represented by [[matrix|matrices]] themselves rather than just [[vector]]s. This is so we preserve geometric information.

Because of this, we must use tensors, which is a natural generalisation of matrices. $n$ matrices are represented by a single tensor in $\mathbb{R}^{n\times m\times d}$.