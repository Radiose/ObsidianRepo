**26**.5/38 points 
1:
b: A predicate is a sentence containing one or more variables which is not a
statement, but the becomes a statement when each variable is assigned
a value from a specified domain. I said  - becomes a statement when quantified or given variables. 
c:
Note that introducing notation for substatements involves this s(q): q > 1. This is used all the time in proofs. 
d:Mistake on negation of a implication. Note that the for $p \implies q$, the negation is $p \land \neg q$. You should not negate p.
e:functional completeness. Not much you can do. Check given equivalence laws to see if theres a simple answer next time. 

2:
A: minor mistake. Not much you can do.
2b: Definition of relations are $(a,b) \subset R \iff$
2d: write down all the subsets that are functions can be done as $\{ (a,1)(a,2) \}$
2e: element proof was not constructed properly. Revisit correct method. 

3:
![[Pasted image 20260501075506.png]]
$\forall A \in P(U) \forall f,g \in F(A)$ are the quantifiers. 

All proofs that follow from this must state out an A, an f and a g. Let $A \in P(U)$, let $f,g \in F(A)$

[[proof by contradiction]] is not the same as proof by counterexample 

4:
Got toggle plus one method wrong. 
Fucked up the addition in hexadecimal. 


For final exam:
STUDOCU previous final exams are the only answer. You need previous exams to practice on. 