2:
create a function g^n that is defined as $f^{n-2}$. From there, the inductive case will utilise $g^{n-1}$, which we have proven is equal to $n-1\cdot g^{n-2}(1-0)^{whatever \exp }$ and thus $\frac{1}{(n-1)n}$ will be multiplied by $(n+1)$  from factorial denominator, and also divided by $n-1$ from the above identity.


3:
b: 
$span(\phi_{1},\phi_{2})=\ker(T^v)$

let $\gamma \in span(\phi_{1},\phi_{2})$
$\implies T^v(\gamma)=0$
$\implies \gamma \circ T=0$
Also note $\phi_{i}:\mathbb{F}[x]_{\leq 2}\to \mathbb{F}$
With thatin mind 

let $p \in range(T)$
We want to show $p \in \ker(\phi_{1})\cap \ker(\phi_{2})$
$\implies \exists \rho_{0}$ such that $T(\rho_{0})=p$
 $\phi_{1}(T(\rho_{0}))=0$ because $\phi_{1}$ is in the kernel of $T$
Do we need a linear independence argument - dont think so.


