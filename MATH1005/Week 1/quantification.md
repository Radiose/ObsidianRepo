---
{}
---
quantification
there are two way to turn a [[predicate]] into a [[Statement]]. 
Specify a __
__

![[The universal quantifier]]

![[The existential quantifier]]

Notation for implies and if and only if
$p(x) \to q(x)$ is short for $\forall x p(x) \to q(x)$
$\forall x (p(x) \to q(x))$
The negation of 
$\neg(\forall x(p(x)))\equiv \exists x \neg(p(x))$
$\neg(\exists x(p(x)))\equiv \forall x \neg(p(x))$

so essentially just switch exists and for all

