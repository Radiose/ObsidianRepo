set builder notation 
set builder notation uses a [[predicate]] p(x) and a predicate domain D.
we write $\{ x\in D|p(x)\}$ which can be read as "the [[Set]] of all x in D for which p(x) is true"
note: the use of | is not universal in set builder notation. Often : and ; are used instead.