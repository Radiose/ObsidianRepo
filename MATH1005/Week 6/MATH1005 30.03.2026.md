---
{}
---
![[Matrix]]

some [[Matrix]] notation 
$M_{m\times n}(S)$ represents the set of all mxn matrices over S, where s is a [[Set]]
for a square matrix, $M_{n}(S)$
$a_{i,j} \in S$ represents the entry in row *i*, column *j* of A
row is always before columns

[[Sequence]]s are 1 dimensional data structures, while [[Matrix|matrices]] are two dimensional 
come back 

A [[relation]] $R \subset \{1,\dots,n \} \times \{ 1, \dots ,n\}$ can be represented be a matrix $a_{i,j} = 1 \iff iRj$
![[Pasted image 20260330123159.png]]


[[vector]]
$\lambda$ is used to denote scalar products of vectors 
$\forall \lambda \in \mathbb{Q} \ \lambda x = \lambda(x_{1},x_{2}\dots,x_{n})$


Addition of vectors must always be the same shape (row and column) 



![[Matrix vector product]]


Provided matrices have the same shape, and that the elements in them have the same data type, then addition and multiplication of matrices are the same as vectors
![[Linear functions]]


![[Matrix multiplication]]



![[Identity matrix]]
![[Inverse matrix]]