---
{}
---
Sequence 

Mathematical definition:
A sequence is a [[function]] $f$ thats domain is the set of natural numbers. Typically we use $f(n) = a_{n}$ to denote the $n$th element in the sequence. 


Let $s$ be a set and $i \subset \mathbb{Z}_{\ge_{0}}$ a function $a: I \to S$ is called a sequence in S. A sequence is a structure that allows us to think about elements of a set occurring in some order.


Sequences have two forms of notation 
$a: I \to S,\ n ↦ a_{n}$ - [[function]] notation, note that ↦ is the mapping of an element from one set to another element. - the element mapping from $n\in\mathbb{N} \mapsto a_n$ is what gives it its order. $n$ can be thought of as the index, mapping directly to an equivalent.

$(a_{n})_{n \in I} \subset S$ - [[Sequence]] notation 
The sequence notation indicates that the function can be represented as an ordered tuple. The notation is trying to tell us that the elements An come from the set S. The sequence itself is not a subset of S, but the range of the sequence function is a subset of its codomain S. 

$I = \{ 1,2,3 \}: (a_{n)_{n\in I}}=(a_{1},a_{2},a_{3})$ 

some sequences 
Interest rate 
$(a_{n})_{n\in \mathbb{N}} \subset \mathbb{Q}$ $a_{n}$ is interest rate at time n

U set of users 
$(a_{n})_{n\in \mathbb{N}} \subset \mathbb{Z}_{>0}\times \mathbb{Z}_{\ge 0}\times \mathbb{Z}_{\ge 0} \subset U$: a list of 5 users.

# MATH1116 notation 
The notation in these notes is significantly weaker than what is wanted in MATH1116
For a sequence of [[real number|reals]], we write $(f_{n})_{n \in \mathbb{N}} \in \mathbb{R}^\mathbb{N}$ instead of 
$$f:\mathbb{N} \to \mathbb{R} \\  $$$$n \mapsto f_{n}$$



![[Explicit definition of a Sequence]]

![[implicit definition of a sequence]]


![[Convergence of a sequence]]