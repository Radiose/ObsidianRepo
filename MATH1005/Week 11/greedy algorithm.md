greedy algorithm 
A greedy algorithm is an iterative algorithm, that only takes into consideration an optima at each current step. For example, in [[weighted graph]]s, a good example is the nearest neighbour algorithm.

![[Nearest neighbour algorithm]]